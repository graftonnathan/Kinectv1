<UserControl x:Class="Kinectv1.UI.Settings.WebRtcSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">
    <UserControl.Resources>
        <SolidColorBrush x:Key="WindowBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBackground" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="SurfaceBackgroundLight" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF464647"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="TextMuted" Color="#FF999999"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF107C10"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FFFF8C00"/>
        <Style x:Key="ThemedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
        </Style>
        <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style x:Key="ThemedCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>
    </UserControl.Resources>
    <Grid Background="{DynamicResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="WebRTC Voice Settings" Foreground="{DynamicResource TextPrimary}" FontSize="16" FontWeight="SemiBold"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
            <StackPanel>
                <Border Background="{DynamicResource SurfaceBackground}" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Connection Status" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Status: " Foreground="{DynamicResource TextMuted}" FontSize="12" Width="80"/>
                            <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="{DynamicResource TextMuted}" Margin="0,0,8,0"/>
                            <TextBlock x:Name="ConnectionStatusText" Text="Not Started" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Clients: " Foreground="{DynamicResource TextMuted}" FontSize="12" Width="80"/>
                            <TextBlock x:Name="ClientCountText" Text="0 connected" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Audio In: " Foreground="{DynamicResource TextMuted}" FontSize="12" Width="80"/>
                            <TextBlock x:Name="AudioInStatsText" Text="--" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource SurfaceBackground}" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Settings" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Enable WebRTC Voice" Foreground="{DynamicResource TextSecondary}" Width="200" VerticalAlignment="Center"/>
                            <CheckBox x:Name="EnabledCheckBox" Style="{StaticResource ThemedCheckBoxStyle}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Port (HTTP)" Foreground="{DynamicResource TextSecondary}" Width="200" VerticalAlignment="Center"/>
                            <TextBox x:Name="PortTextBox" Style="{StaticResource ThemedTextBoxStyle}" Width="100"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Enable HTTPS (for mic)" Foreground="{DynamicResource TextSecondary}" Width="200" VerticalAlignment="Center"/>
                            <CheckBox x:Name="HttpsEnabledCheckBox" Style="{StaticResource ThemedCheckBoxStyle}" ToolTip="Required for microphone access on iOS Safari"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="HTTPS Port" Foreground="{DynamicResource TextSecondary}" Width="200" VerticalAlignment="Center"/>
                            <TextBox x:Name="HttpsPortTextBox" Style="{StaticResource ThemedTextBoxStyle}" Width="100"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="Join URL" Foreground="{DynamicResource TextSecondary}" Width="200" VerticalAlignment="Center"/>
                            <TextBox x:Name="JoinUrlTextBox" Style="{StaticResource ThemedTextBoxStyle}" IsReadOnly="True" Width="250"/>
                            <Button Content="Copy" Margin="8,0,0,0" Style="{StaticResource ThemedButtonStyle}" Click="CopyUrlButton_Click"/>
                        </StackPanel>
                        <Border Background="{DynamicResource SurfaceBackgroundLight}" CornerRadius="4" Padding="12" Margin="0,8,0,0" x:Name="CertInfoPanel" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="Self-Signed Certificate" FontWeight="SemiBold" Foreground="{DynamicResource AccentOrange}" Margin="0,0,0,8"/>
                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{DynamicResource TextSecondary}" Text="Accept the certificate warning in your browser."/>
                                <Button Content="Regenerate Certificate" Margin="0,8,0,0" Style="{StaticResource ThemedButtonStyle}" Click="RegenerateCertButton_Click"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource SurfaceBackground}" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="Recent Speakers" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                            <Button x:Name="RefreshSpeakersButton" Content="Refresh" Margin="20,0,0,0" Style="{StaticResource ThemedButtonStyle}" Click="RefreshSpeakersButton_Click" Padding="8,4"/>
                        </StackPanel>
                        <TextBlock x:Name="NoSpeakersText" Text="No speakers detected yet" Foreground="{DynamicResource TextMuted}" FontSize="12"/>
                        <ItemsControl x:Name="SpeakersListControl" Visibility="Collapsed"/>
                    </StackPanel>
                </Border>
                <Border Background="{DynamicResource SurfaceBackground}" CornerRadius="4" Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Customize Web Client" Foreground="{DynamicResource TextPrimary}" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,12"/>
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Open Web Folder" Style="{StaticResource ThemedButtonStyle}" Click="OpenWebFolderButton_Click" Margin="0,0,8,0"/>
                            <Button Content="Reload" Style="{StaticResource ThemedButtonStyle}" Click="ReloadWebContentButton_Click"/>
                        </StackPanel>
                        <TextBlock x:Name="WebFolderPathText" Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,8,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
                <TextBlock x:Name="StatusText" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,10"/>
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Style="{StaticResource ThemedButtonStyle}" Background="{DynamicResource AccentGreen}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
