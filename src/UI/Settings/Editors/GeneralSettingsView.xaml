<UserControl x:Class="Kinectv1.UI.Settings.Editors.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Kinectv1.UI.Settings.Editors"
             Background="{DynamicResource WindowBackground}">

    <UserControl.Resources>
        <!-- Style definitions matching the main window theme -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="Height" Value="32"/>
        </Style>

        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel MaxWidth="500">
            <!-- Header -->
            <TextBlock Text="General Settings" 
                       Style="{StaticResource SectionHeaderStyle}"
                       FontSize="20"
                       Margin="0,0,0,20"/>

            <!-- Theme Setting -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Theme:" 
                           Style="{StaticResource LabelStyle}"
                           Grid.Row="0" Grid.Column="0"
                           Width="120"/>
                
                <ComboBox x:Name="ThemeComboBox"
                          Style="{StaticResource ComboBoxStyle}"
                          Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding ThemeOptions}"
                          SelectedValue="{Binding IsDarkMode, Mode=TwoWay}"
                          SelectedValuePath="Value"
                          DisplayMemberPath="DisplayName"
                          Margin="10,0,0,0"/>
            </Grid>

            <!-- Language Setting -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Language:" 
                           Style="{StaticResource LabelStyle}"
                           Grid.Row="0" Grid.Column="0"
                           Width="120"/>
                
                <ComboBox x:Name="LanguageComboBox"
                          Style="{StaticResource ComboBoxStyle}"
                          Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding LanguageOptions}"
                          SelectedValue="{Binding SelectedLanguage, Mode=TwoWay}"
                          SelectedValuePath="Code"
                          DisplayMemberPath="DisplayName"
                          Margin="10,0,0,0"/>
            </Grid>

            <!-- Logging Level Setting -->
            <Grid Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Logging Level:" 
                           Style="{StaticResource LabelStyle}"
                           Grid.Row="0" Grid.Column="0"
                           Width="120"/>
                
                <ComboBox x:Name="LoggingLevelComboBox"
                          Style="{StaticResource ComboBoxStyle}"
                          Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding TelemetryLevelOptions}"
                          SelectedValue="{Binding SelectedTelemetryLevel, Mode=TwoWay}"
                          SelectedValuePath="Level"
                          DisplayMemberPath="DisplayName"
                          Margin="10,0,0,0"/>
            </Grid>

            <!-- Dirty State Indicator -->
            <TextBlock Text="● Unsaved changes" 
                       Foreground="{DynamicResource AccentOrange}"
                       FontSize="12"
                       Margin="0,0,0,15"
                       Visibility="{Binding IsDirty, Converter={x:Static local:BooleanToVisibilityConverter.Instance}}"/>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Left">
                <Button Content="Apply" 
                        Style="{StaticResource ButtonStyle}"
                        Command="{Binding ApplyCommand}"
                        Margin="0,0,10,0"/>
                
                <Button Content="Save" 
                        Style="{StaticResource ButtonStyle}"
                        Command="{Binding SaveCommand}"
                        Margin="0,0,10,0"/>
                
                <Button Content="Reset" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding ResetCommand}"/>
            </StackPanel>

            <!-- Help Text -->
            <TextBlock TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondary}"
                       FontSize="12"
                       Margin="0,20,0,0">
                <Run Text="Theme: "/>
                <Run Text="Controls the application's visual appearance."/>
                <LineBreak/>
                <Run Text="Language: "/>
                <Run Text="Sets the application language (restart may be required)."/>
                <LineBreak/>
                <Run Text="Logging Level: "/>
                <Run Text="Controls the verbosity of telemetry and logging output."/>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>