<UserControl x:Class="Kinectv1.ModelsSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             FontSize="12">
    
    <UserControl.Resources>
        <!-- Inherit theme resources from main application -->
        <SolidColorBrush x:Key="WindowBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="SurfaceBackground" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="SurfaceBackgroundLight" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF464647"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFE0E0E0"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF107C10"/>
        <SolidColorBrush x:Key="AccentOrange" Color="#FFFF8C00"/>
        <SolidColorBrush x:Key="AccentRed" Color="#FFE74C3C"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#FF8B5CF6"/>

        <!-- Button Style -->
        <Style x:Key="ThemedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundLight}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="#FFFFFF"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBlue}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="6"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBlue}"/>
                    <Setter Property="Background" Value="{DynamicResource SurfaceBackgroundLight}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBlue}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="ThemedComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Focusable="false" IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0" Margin="8,3,0,3" Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" VerticalAlignment="Center" HorizontalAlignment="Left" IsHitTestVisible="False" />
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource TextPrimary}" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource SurfaceBackgroundLight}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}" Margin="8,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>

                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}">
                                    <Border Name="DropDownBorder" Background="{DynamicResource SurfaceBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Margin="0,1,0,0">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True">
                                                <StackPanel.Resources>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ComboBoxItem">
                                                                    <Border Name="Border" Padding="8,4" CornerRadius="2">
                                                                        <ContentPresenter>
                                                                            <ContentPresenter.Resources>
                                                                                <Style TargetType="TextBlock">
                                                                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                                                                    <Setter Property="FontWeight" Value="Medium"/>
                                                                                </Style>
                                                                            </ContentPresenter.Resources>
                                                                        </ContentPresenter>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsHighlighted" Value="True">
                                                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentBlue}"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsSelected" Value="True">
                                                                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource SurfaceBackgroundLight}"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </StackPanel.Resources>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style (match DiscordSettingsView) -->
        <Style x:Key="ThemedCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource WindowBackground}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Header -->
                <TextBlock Text="General Settings" FontSize="16" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,12"/>

                <!-- TTS Model Settings -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Text-to-Speech (TTS)" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <!-- TTS Enabled -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Enable TTS" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <CheckBox Grid.Column="1" Name="TtsEnabledCheckBox" Style="{StaticResource ThemedCheckBoxStyle}"/>
                        </Grid>

                        <!-- TTS Model Path -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Model File" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="TtsModelPathTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseTtsModelButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseTtsModelButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- TTS Model Folder -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Model Folder" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="TtsModelFolderTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseTtsModelFolderButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseTtsModelFolderButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- TTS Vocoder Path -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Vocoder File" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="TtsVocoderPathTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseTtsVocoderButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseTtsVocoderButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- TTS Voice Selection -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Voice" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <ComboBox Grid.Column="1" Name="TtsVoiceComboBox" Height="30"
                                      Style="{StaticResource ThemedComboBoxStyle}"
                                      SelectionChanged="TtsVoiceComboBox_SelectionChanged"/>
                        </Grid>

                        <!-- TTS Output Device -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Output Device" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <ComboBox Grid.Column="1" Name="TtsOutputDeviceComboBox" Height="30"
                                      Style="{StaticResource ThemedComboBoxStyle}"/>
                        </Grid>

                        <!-- Local TTS Volume -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Local Volume" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="LocalVolumeSlider" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="LocalVolumeValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- Discord TTS Volume -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Discord Volume" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="DiscordVolumeSlider" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="DiscordVolumeValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- Execution Mode -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Execution Mode" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <ComboBox Grid.Column="1" Name="ExecutionModeComboBox" Height="30"
                                      Style="{StaticResource ThemedComboBoxStyle}"
                                      SelectionChanged="ExecutionModeComboBox_SelectionChanged">
                                <ComboBoxItem Content="CPU" Tag="CPU"/>
                                <ComboBoxItem Content="GPU (CUDA)" Tag="GPU"/>
                            </ComboBox>
                        </Grid>

                        <!-- Voice Test -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Test Phrase" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="TestPhraseTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center" Text="Hello, this is a test of the text to speech system."/>
                            <Button Grid.Column="2" Name="TestVoiceButton" Content="Test" 
                                    Width="80" Height="30" Click="TestVoiceButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- TTS Pacing: Speed -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Speed (x)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="TtsSpeedSlider" Minimum="50" Maximum="200" TickFrequency="5" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="TtsSpeedValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- TTS Pacing: Trim Silence Threshold -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Trim Silence Threshold" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="TrimSilenceThresholdSlider" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="TrimSilenceThresholdValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- TTS Pacing: Trim Leave (ms) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Trim Leave (ms)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="TtsTrimLeaveSlider" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="TtsTrimLeaveValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- TTS Pacing: Trim Max (ms) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Trim Max (ms)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="TtsTrimMaxSlider" Minimum="50" Maximum="3000" TickFrequency="50" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="TtsTrimMaxValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- TTS Pacing: Padding (ms) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Padding (ms)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <Slider Grid.Column="1" Name="TtsPaddingSlider" Minimum="0" Maximum="200" TickFrequency="10" IsSnapToTickEnabled="True"/>
                            <TextBlock Grid.Column="2" Name="TtsPaddingValueText" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                        </Grid>

                        <!-- IPA Timeouts -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="IPA Timeouts (ms)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="TtsIpaServiceTimeoutTextBox" Height="30" Margin="0,0,8,0" Style="{StaticResource ThemedTextBoxStyle}" ToolTip="Service timeout (200–5000 ms)"/>
                            <TextBox Grid.Column="2" Name="TtsIpaOneShotTimeoutTextBox" Height="30" Margin="0,0,8,0" Style="{StaticResource ThemedTextBoxStyle}" ToolTip="One‑shot fallback timeout (200–5000 ms)"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- STT & Models Settings -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Speech-to-Text (STT)" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <!-- STT Model Path -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Vosk Model" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="SttModelPathTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseSttModelButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseSttModelButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- Microphone (STT) Input Device -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Microphone (STT)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <ComboBox Grid.Column="1" Name="MicInputComboBox" Height="30"
                                      Style="{StaticResource ThemedComboBoxStyle}"
                                      SelectionChanged="MicInputComboBox_SelectionChanged"/>
                        </Grid>

                        <!-- Voice Match Threshold -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Voice Match Threshold (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="SpeakerMatchThresholdTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>

                        <!-- Speaker Embedding Model Path -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Speaker Model" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="SpeakerModelPathTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseSpeakerModelButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseSpeakerModelButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>

                        <!-- ArcFace Model Path -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="ArcFace Model" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="ArcFaceModelPathTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"
                                     VerticalContentAlignment="Center"/>
                            <Button Grid.Column="2" Name="BrowseArcFaceModelButton" Content="Browse" 
                                    Width="80" Height="30" Click="BrowseArcFaceModelButton_Click"
                                    Style="{StaticResource ThemedButtonStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Audio Settings -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Audio" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <!-- Voice Threshold -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Voice Threshold (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="AudioVoiceThresholdTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>

                        <!-- High Confidence Threshold -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="High Confidence Threshold (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="AudioVoiceHighThresholdTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>

                        <!-- Confidence Logging -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Confidence Logging" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <CheckBox Grid.Column="1" Name="VoiceConfidenceLoggingCheckBox" Content="Enable"/>
                        </Grid>

                        <!-- VAD Threshold (audio) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="VAD Threshold (audio)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="AudioVadThresholdTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>

                        <!-- Buffer Size -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Buffer Size" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="AudioBufferSizeTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- VAD Settings -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="VAD" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="VAD Threshold" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="VadThresholdTextBox" Margin="0,0,8,0" 
                                     Style="{StaticResource ThemedTextBoxStyle}" Height="30"/>
                        </Grid>

                        <!-- Wake word requirement toggle -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Require wake word (Local)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <CheckBox Grid.Column="1" Name="RequireWakeWordCheckBox"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Face Recognition Settings -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Face Recognition" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8"/>

                        <!-- Face Threshold (0..1) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Face Threshold (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="FaceThresholdTextBox" Height="30" 
                                     Style="{StaticResource ThemedTextBoxStyle}"/>
                        </Grid>

                        <!-- Fusion Face Weight (0..1) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Fusion Face Weight (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="FusionFaceWeightTextBox" Height="30" 
                                     Style="{StaticResource ThemedTextBoxStyle}"/>
                        </Grid>

                        <!-- Fusion Voice Weight (0..1) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Fusion Voice Weight (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="FusionVoiceWeightTextBox" Height="30" 
                                     Style="{StaticResource ThemedTextBoxStyle}"/>
                        </Grid>

                        <!-- Fusion Decay Half-Life (ms) -->
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Fusion Decay Half-Life (ms)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="FusionHalfLifeTextBox" Height="30" 
                                     Style="{StaticResource ThemedTextBoxStyle}"/>
                        </Grid>

                        <!-- Fusion Unknown Threshold (0..1) -->
                        <Grid Margin="0,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="220"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Fusion Unknown Threshold (0..1)" VerticalAlignment="Center" 
                                       Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Grid.Column="1" Name="FusionUnknownThresholdTextBox" Height="30" 
                                     Style="{StaticResource ThemedTextBoxStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Name="ValidateButton" Content="Validate" 
                            Width="120" Height="32" Margin="0,0,8,0" Click="ValidateButton_Click"
                            Style="{StaticResource ThemedButtonStyle}"/>
                    <Button Name="SaveButton" Content="Save" 
                            Width="120" Height="32" Margin="0,0,8,0" Click="SaveButton_Click"
                            Style="{StaticResource ThemedButtonStyle}"/>
                    <Button Name="ResetButton" Content="Reset to Defaults" 
                            Width="150" Height="32" Click="ResetButton_Click"
                            Style="{StaticResource ThemedButtonStyle}"/>
                </StackPanel>

                <!-- Status Display -->
                <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0" CornerRadius="0" 
                        Background="{DynamicResource WindowBackground}" Margin="0,12,0,0">
                    <TextBlock Name="StatusTextBlock" Text="Ready - Configure your TTS and STT model paths above"
                               Padding="6" FontSize="12" Foreground="{DynamicResource TextSecondary}"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>