# Copilot Agent Task
version: 1
name: "Audit App.config and binding redirects"
goal: |
  Validate App.config / binding redirects for .NET Framework target.
  
  Steps:
  - Ensure supportedRuntime matches target framework.
  - Add bindingRedirects for common libs if needed.
  - Confirm presence of StartupUri for WPF apps (if applicable).

context:
  repo_paths:
    - Kinectv1.sln
    - Kinectv1.csproj
    - tests/Tests.csproj
    - tools/AsrOffline.csproj

acceptance_criteria:
  - Changes compile locally with `dotnet build` (Debug, Any CPU).
  - All referenced files in .csproj exist and relative paths are correct.
  - No leftover merge conflict markers (`<<<<<<<`, `>>>>>>>`) in the repo.
  - CI passes on push and PR.
