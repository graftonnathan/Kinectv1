# Copilot Agent Task
version: 1
name: "Validate external config references"
goal: |
  Verify and fix external config file references (e.g., log4net, NLog, custom XML).
  
  Items to validate:
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  - Properties/Settings.settings -> (Default)
  
  Steps:
  - Ensure referenced config files exist in the repo and are copied to output (Copy to Output Directory).
  - If paths are wrong, make them relative to the project and cross-platform safe.
  - Confirm runtime loads these configs without exceptions.

context:
  repo_paths:
    - Kinectv1.sln
    - Kinectv1.csproj
    - tests/Tests.csproj
    - tools/AsrOffline.csproj

acceptance_criteria:
  - Changes compile locally with `dotnet build` (Debug, Any CPU).
  - All referenced files in .csproj exist and relative paths are correct.
  - No leftover merge conflict markers (`<<<<<<<`, `>>>>>>>`) in the repo.
  - CI passes on push and PR.
