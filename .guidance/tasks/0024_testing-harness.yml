task: 0024 - Offline testing harness (WAV→ASR, Discord frames→STT, pruning snapshots)
why:
  symptoms:
    - "Difficult to validate fixes without manual runs."
  diagnosis: "Add small deterministic fixtures and golden outputs."
scope:
  repo: graftonnathan/Kinectv1
  branch: dev
changes:
  - "tests/AsrGoldenTests.cs: run short WAVs through VoiceProcessor; compare transcripts."
  - "tests/DiscordFrameFeed.cs: synthetic 48k frames → pipeline; assert no 'chunk too large'."
  - "tests/PruningSnapshotTests.cs: identity fusion, VAD thresholds, debounce behavior."
acceptance:
  - "CI green on tests; failures point to specific stage regressions."
