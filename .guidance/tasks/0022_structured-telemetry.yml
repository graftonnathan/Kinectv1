task: 0022 - Add structured telemetry & key counters/timers
why:
  symptoms:
    - "Hard to see regressions; many Console.WriteLine entries."
  diagnosis: "Introduce structured logs (level, component, fields) and lightweight counters/timers."
scope:
  repo: graftonnathan/Kinectv1
  branch: dev
changes:
  - "Telemetry.cs: Info/Warn/Error/Metric(JSON) with context fields."
  - "Emit metrics for: discord.join.ms, audio.resample.ms, asr.latency.ms, tts.synth.ms, frames dropped."
  - "Rotate NDJSON logs; sampling control in AppSettings."
acceptance:
  - "metrics log file present with counters updating during a 1m run."
  - "Console output quieter but retains summaries."
