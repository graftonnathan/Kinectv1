# Copilot Agent Task
version: 1
name: "Implement Paths settings editor"
goal: |
  Provide a `PathsSettingsView` with FolderPicker/FilePicker controls for model paths, cache dir, and exports folder.

context:
  repo_paths:
    - ./

tasks:
  - Add `/src/UI/Settings/Editors/PathsSettingsView.xaml` with textboxes + Browse buttons.
  - Implement path validator and offer Create Folder flow.
  - Relative vs absolute path toggle.

acceptance_criteria:
  - Browse opens OS picker and updates textbox.
  - Invalid paths show warning.
  - Saved values are used on next launch.
