# Copilot Agent Task
version: 1
name: "Implement Models settings editor"
goal: |
  Provide a `ModelsSettingsView` for TTS/STT model paths and execution mode.

context:
  repo_paths:
    - ./

tasks:
  - Add pickers for model files/folders.
  - Add dropdown for execution mode CPU/GPU.
  - Add voice list for TTS and test phrase.

acceptance_criteria:
  - Model paths validate and persist.
  - Execution mode switches affect runtime.
  - Optional voice test works.
