# Copilot Agent Task
version: 1
name: "Implement Integrations settings editor"
goal: |
  Provide an `IntegrationsSettingsView` for Discord and telemetry.

context:
  repo_paths:
    - ./

tasks:
  - Fields for Discord token, guild/channel IDs.
  - Toggle telemetry.
  - Test connection button.

acceptance_criteria:
  - Secrets masked.
  - Validation prevents malformed IDs.
  - Test connection reports gracefully.
