# Copilot Agent Task
version: 1
name: "Wire Apply/Save/Cancel/Defaults"
goal: |
  Implement Apply, Save, Cancel, and Restore Defaults with dirty-state.

context:
  repo_paths:
    - ./

tasks:
  - Implement edit/cancel/commit flow.
  - Track dirty state.
  - Prompt on close if dirty.
  - Restore Defaults uses Defaults.cs.

acceptance_criteria:
  - Dirty indicator updates.
  - Cancel reverts unsaved changes.
  - Restore Defaults works per-category.
